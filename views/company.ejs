<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aigle Air - Ad Request</title>
    <!-- External CSS -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap">
    <style>
        body {
            background: radial-gradient(circle, #1a202c, #0f141b);
            color: #e2e8f0;
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            color: #4fd1c5;
            margin: 20px 0;
            font-size: 2.5rem;
            text-shadow: 0 0 10px #4fd1c5, 0 0 20px #38b2ac;
        }

        form {
            width: 90%;
            max-width: 600px;
            margin: 30px auto;
            background: #2d3748;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
            color: #81e6d9;
        }

        input, textarea, select, button {
            width: 100%;
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #4a5568;
            border-radius: 5px;
            background: #1a202c;
            color: #e2e8f0;
            font-size: 1rem;
        }

        input[type="file"] {
            padding: 5px;
        }

        button {
            background: #4fd1c5;
            color: #1a202c;
            font-weight: bold;
            cursor: pointer;
        }

        button:hover {
            background: #38b2ac;
        }
    </style>
</head>
<body>
    <h1>Ad Request Form</h1>
    <form action="/company" method="POST" enctype="multipart/form-data">
        <label for="companyName">Company Name:</label>
        <input type="text" id="companyName" name="companyName" placeholder="Enter your company name" required>

        <label for="description">Description:</label>
        <textarea id="description" name="description" rows="4" placeholder="Brief description about your ad" required></textarea>

        <label for="adFile">Upload Ad (MP4):</label>
        <input type="file" id="adFile" name="adFile" accept="video/mp4" required>

        <label for="location">Select Devices:</label>
        <select id="location" name="devices[]" multiple required>
            <% systems.forEach(system => { %>
                <option value="<%= system.macAddress %>"><%= system.name %> - <%= system.location %></option>
            <% }); %>
        </select>

        <button type="submit">Submit Ad Request</button>
    </form>
</body>
</html>
